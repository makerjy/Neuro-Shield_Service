services:
  backend:
    platform: linux/amd64
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: stage3-future-backend
    environment:
      STAGE3_PROJECT_ROOT: /workspace
    volumes:
      - ../../../:/workspace:ro
    ports:
      - "8002:8000"
    restart: unless-stopped
